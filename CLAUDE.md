# Claude Project Entry Point

Follow `AGENTS.md` as the canonical agent instruction file.

Read `memory/index.json` and every file in its `read_order` before working on
the project. Do not create a separate Claude-only memory or duplicate project
state in this file. Update the shared JSON memory according to the protocol in
`AGENTS.md`.
